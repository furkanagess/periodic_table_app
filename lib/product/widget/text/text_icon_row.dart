import 'package:flutter/material.dart';

class TextIconRow extends StatelessWidget {
  const TextIconRow({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
